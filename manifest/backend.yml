name: TODO MVC Application

entities:
  Todo:
    properties:
      - {name: "title", type: string}
      - {name: "description", type: string} # Add this line
      - {name: "completed", type: boolean}
      - {name: "order", type: number}
      - {name: "createdAt", type: timestamp}
  TodoList:
    properties:
      - {name: "title", type: string}
      - {name: "createdAt", type: timestamp}